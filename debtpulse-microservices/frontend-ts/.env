# Base path for API calls. Left as "/api" so the Vite dev proxy (and a production gateway/ingress)
# handle routing. To hit the gateway directly instead, set VITE_API_BASE=http://localhost:9090/api.
VITE_API_BASE=/api
VITE_API_TARGET=http://localhost:9090
