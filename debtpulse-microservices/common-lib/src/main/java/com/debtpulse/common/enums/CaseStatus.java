package com.debtpulse.common.enums;

/** Lifecycle state of a legal case (2.6). */
public enum CaseStatus {
    FILED,
    PENDING,
    HEARING_SCHEDULED,
    DECREED,
    DISMISSED,
    WITHDRAWN,
    SETTLED
}
