package com.debtpulse.common.enums;

/** Result of a contact attempt. */
public enum ContactOutcome {
    CONNECTED,
    NOT_CONNECTED,
    BUSY_TONE,
    NUMBER_INVALID,
    DISCONNECTED,
    REFUSED
}
