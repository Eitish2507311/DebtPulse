package com.debtpulse.common.enums;

/** Channel used for a borrower contact attempt (2.3 Contact &amp; Follow-Up Management). */
public enum ContactChannel {
    CALL,
    SMS,
    EMAIL,
    VISIT,
    LETTER
}
