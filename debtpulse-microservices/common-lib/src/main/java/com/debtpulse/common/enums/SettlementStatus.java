package com.debtpulse.common.enums;

/** Lifecycle of a {@code SettlementProposal}. */
public enum SettlementStatus {
    DRAFT,
    PENDING_APPROVAL,
    APPROVED,
    REJECTED,
    PAID,
    EXPIRED
}
